name: Release

on:
  workflow_dispatch:
    inputs:
      publish:
        description: "Publish to npm"
        required: false
        default: "false"
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve project directory
        id: paths
        shell: bash
        run: |
          if [ -f package.json ]; then
            echo "workdir=." >> "$GITHUB_OUTPUT"
          elif [ -f packages/superpower-agent/package.json ]; then
            echo "workdir=packages/superpower-agent" >> "$GITHUB_OUTPUT"
          else
            echo "Unable to locate package.json for superpower-agent"
            exit 1
          fi

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          registry-url: "https://registry.npmjs.org"
          always-auth: true

      - name: Install dependencies
        run: npm install
        working-directory: ${{ steps.paths.outputs.workdir }}

      - name: Run checks
        run: npm run check
        working-directory: ${{ steps.paths.outputs.workdir }}

      - name: Validate package
        run: npm pack --dry-run
        working-directory: ${{ steps.paths.outputs.workdir }}

      - name: Verify npm auth configuration
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.publish == 'true')
        shell: bash
        working-directory: ${{ steps.paths.outputs.workdir }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -n "${NODE_AUTH_TOKEN:-}" ]; then
            echo "NPM_TOKEN detected. Proceeding with npm publish."
            exit 0
          fi
          echo "::error::Missing NPM_TOKEN repository secret. Configure Settings > Secrets and variables > Actions > New repository secret named NPM_TOKEN with an npm automation/granular publish token, or switch this workflow to npm trusted publishing."
          exit 1

      - name: Publish to npm
        if: startsWith(github.ref, 'refs/tags/v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.publish == 'true')
        run: npm publish --access public --provenance
        working-directory: ${{ steps.paths.outputs.workdir }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
